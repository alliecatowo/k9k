# K9s parity ledger

This is the implementation ledger for K9k, a native macOS application whose behavioural reference is K9s. K9k replaces the TUI with native SwiftUI while keeping Kubernetes semantics in the bundled Go helper.

## Reference pin

| Item | Value |
| --- | --- |
| Upstream | `https://github.com/derailed/k9s` |
| Reference revision | `b5d728c068b3584eb8530fb1f6cc51b13a67b39c` |
| Revision date | 2026-08-02T09:08:42-06:00 |
| Revision subject | `chore(deps): bump golang from 1.26.4-alpine3.24 to 1.26.5-alpine3.24 (#4115)` |
| Upstream default branch at fetch | `master` at the reference revision |
| Current release tag available in fetched repository | `v0.51.0` |
| Source location | `.vendor/k9s` |
| K9s module Go directive | `go 1.25.8` (`.vendor/k9s/go.mod`) |
| K9s stated minimum | Go 1.23.x or later (`.vendor/k9s/README.md`) |
| Kubernetes modules | `k8s.io/{api,apimachinery,client-go,metrics}` `v0.35.3`; `cli-runtime` and `kubectl` `v0.35.1`; `apiextensions-apiserver` `v0.35.3` |

The fetched pin, rather than a README or release-era blog post, is the authority for every row below. K9k's backend uses the same `api`, `apimachinery`, and `client-go` `v0.35.3` family; it is not linked to or dependent on the K9s executable.

## Status vocabulary and audit boundary

**Complete** means the user-visible workflow is available through the native app and its bundled backend, with an automated test. **Partial** means a meaningful code path exists but is not the complete K9s workflow. **Blocked** means the required backend or UI operation does not yet exist. **N/A** means a K9s terminal-only presentation choice intentionally has no direct GUI feature.

Statuses reflect the current early K9k baseline: `Backend/internal/api.Server` dispatches versioned NDJSON requests for context, namespace, discovery, generic resource, scale, events, logs, cancellation, and watch operations; the SwiftUI app supplies a native context/namespace/resource browser, resource inspector, and log viewer. It is intentionally not treated as parity-complete: substantial K9s workflows remain absent. Test entries name required validation; `—` means none exists in the baseline.

## Capability matrix

| Capability | K9s source | K9s behavior | K9k implementation | Status | Tests | Difference |
| --- | --- | --- | --- | --- | --- | --- |
| Kubeconfig, auth, and current context | `internal/client/config.go` (`Config`, `RESTConfig`, `SwitchContext`); `client.go` (`APIClient.SwitchContext`) | Resolves kubeconfig and active context, creates clients, and resets state when context changes. | `Backend/internal/kube/cluster.go` (`Cluster.New`, `reload`, `SelectContext`) uses `clientcmd` and creates typed, dynamic, and discovery clients. Native Settings graphically lists context references, clusters, users, active state, and default namespaces; it can switch, persist a confirmed default-namespace change, rename a context reference, and remove only an inactive context via `clientcmd.ModifyConfig`. Credentials and endpoints remain opaque. | Partial | Context-switch and confirmed context-mutation IPC tests. | Swift never reads kubeconfig or credentials. |
| Context listing and management | `view/context.go` (`Context`, `useContext`); `client/config.go` (`Contexts`, `DelContext`, `RenameContext`) | Lists, selects, renames, and deletes contexts. | `context.list`, `context.select`, `context.rename`, and `context.delete` are dispatched. The graphical context manager confirms renames and inactive-context removal, while active contexts must first be switched away from. | Partial | List/select and context-mutation IPC tests. | GUI uses a context picker/manager rather than `ctx`. |
| Namespace selection | `view/ns.go`; `config/config.go` (`SetActiveNamespace`); `client/client.go` (`ValidNamespaceNames`) | Lists namespaces, supports all namespaces/favourites, and persists active namespace. | `namespace.list` feeds a toolbar picker and `All Namespaces` maps to an empty namespace; selected namespace and locally persisted favourites are available in the toolbar and Settings. | Partial | Namespace list/selection/favourite persistence. | Native picker replaces numeric namespace shortcuts. |
| Discovery and GVR/GVK resolution | `client/gvr.go`, `gvrs.go`; `dao/registry.go` (`Meta.LoadResources`) | Discovers API resources, resolves aliases/GVRs, and handles built-in plus dynamic resources. | `discovery.list` returns group, version, resource, kind, scope, and short names to the native catalog; `parseGVR` preserves identity. | Partial | Fake discovery, including partial failure. | GUI groups discovered resources instead of command aliases. |
| Generic resource list | `dao`, `watch`, `model`, `view/browser.go` (`Browser`) | Continuously lists and displays typed and dynamic resources with K9s column renderers. | `resource.list` dynamically lists a GVR; the native `Table` displays Name, Status, Age, and Kind. No K9s-equivalent renderers/paging. | Partial | Fake dynamic-client list and table UI. | Native `Table` replaces terminal table. |
| Resource watch | `internal/watch`; `model`; `view/browser.go` | Maintains list/watch views and refreshes as objects change. | `resource.watch` emits add/modify/delete events and `ClusterStore` applies them. An unexpected close performs bounded delayed relist/reconnect attempts, then surfaces an explicit Refresh recovery path. | Partial | Add/modify/delete stream lifecycle. | Watches stream to Swift, never from terminal output. |
| Details, YAML, raw fidelity | `view/browser.go` (`viewCmd`); `view/yaml.go`; `model/yaml.go` | Opens structured YAML/raw object views without losing metadata. | The inspector renders sorted, syntax-colored, selectable raw object JSON with Copy; `manifest.get` opens a canonical YAML editor with server-managed fields/status removed and preserves the selected UID for later validation/apply. | Partial | Get/YAML round-trip; manifest canonicalization and native editor load/validation. | Native inspector/editor replaces K9s pages. |
| Filter, label selector, search, sorting | `view/table.go`; `browser.go` (`SetFilter`, `SetLabelSelector`); `view/cmd` | Supports text filtering, labels, column sorting, marks, and history. | Native text search, table sort/multiselection, and a Filter Resources sheet for Kubernetes label and field selectors exist. Selectors apply equally to the initial list and its live watch, are scoped to their current GVR, and can be cleared inline; saved queries and K9s column/custom-view parity do not. | Partial | Selector propagation, selector-scoping, and UI query tests. | Native search/multiselection replace command/filter modes. |
| Generic mutation: patch/edit/delete | `view/browser.go` (`editCmd`, `deleteCmd`); `live_view.go`; `ui/dialog` | Opens editor, confirms destructive work, updates/deletes selection. | `resource.patch` and `resource.delete` are dispatched; delete is confirmation/read-only guarded and preflighted by `SelfSubjectAccessReview`. The native manifest editor validates canonical YAML through mandatory server-side dry run; Apply is separately confirmed, UID-pinned, and access-reviewed before its non-forced SSA write. | Partial | Patch/delete integration, permission/confirmation UI, manifest canonicalization/dry-run/identity IPC tests, and Kind editor validation. | Native sheets/alerts, not `$EDITOR`. |
| Workload views | `view/{workload,dp,sts,ds,rs}.go`; `registrar.go` | Specialises Deployments, StatefulSets, DaemonSets, ReplicaSets, and aggregate workloads. | Generic discovery/list can represent these GVRs; no workload UI/actions. | Partial | Workload fixture/table tests. | Native grouped navigation replaces commands. |
| Pods and containers | `view/{pod,container}.go`; `registrar.go` | Provides pod/container details, logs, shell, attach, transfer, image, ownership, and port-forward actions. | Generic dynamic pod access, native follow-mode logs, a cancellable loopback-only Pod port-forward sheet, and a permission-checked Pod terminal. The inspector shows container image references, and explicit container selection is available for logs and shell; attach, transfer, and ownership traversal remain absent. | Partial | Pod summary/container extraction, log-stream fixture, live loopback tunnel, and interactive Kind shell fixture. | Actions become toolbar/context-menu commands. |
| Logs | `view/log.go` (`Log`, `bindKeys`); `logs_extender.go`; `model/log.go`; `dao/log.go` | Streams logs with container choice, previous/follow/filter/timestamp/wrap/autoscroll/save/copy controls. | `logs.open` streams `logs.data`/`logs.closed` through client-go; the native log sheet selects a container and toggles previous/timestamps/follow before reload, filters locally, wraps or horizontally scrolls lines, filters locally, copies visible lines to the macOS pasteboard, and saves visible output through a native Save panel. | Partial | Server stream/cancellation tests and Kind completed-Job log check. | Native log viewer replaces terminal controls. |
| Exec, shell, attach, debug | `view/exec.go`; `pod.go` (`shellCmd`, `attachCmd`); `container.go`; `xray.go` | Uses Kubernetes exec/attach and K9s shell/debug workflows. | `exec.open` provides direct client-go SPDY Pod exec with ordered binary stdin, base64 output events, resize, cancellation, and exit results. `attach.open` uses the same protected VT transport against `pods/attach`, with a separate `pods/attach` RBAC review and multi-container picker. A confirmed Debug Container sheet calls the native `ephemeralcontainers` subresource with an image and target-container choice; Pod Terminal includes ephemeral containers after refresh. The native terminal uses the pinned SwiftTerm VT/xterm AppKit component for ANSI colors, Unicode, scrollback, selection/copy-paste, keyboard input, and terminal resize. | Partial | Exec/attach/debug lifecycle and validation tests; live Kind one-shot and interactive shell test; native terminal smoke. | Native terminal surface; no TUI suspension. |
| Port forwarding | `view/pf_extender.go`; `pf.go`; `internal/port` | Starts/stops forwards, tracks active ports/URLs, supports benchmark handoff. | `portforward.open` is a cancellable direct client-go SPDY tunnel; native Pod and selector-backed Service actions bind loopback only, display their endpoint, copy the HTTP endpoint, and open it in the default browser. Service forwarding resolves one matching Pod and numeric/named target port before using the same Pod tunnel; selector-less or unresolved services fail clearly. Multiple forwards remain active after their setup sheet closes and a native manager lists each endpoint with Copy/Open/Benchmark/Stop controls. Benchmark runs bounded, cancellable native HTTP requests solely through a selected K9k-owned loopback tunnel and reports completion, failures, rate, mean latency, and total time. Reconnect remains absent. | Partial | Ready/error/cancel protocol tests, Kind HTTP-through-tunnel check, and native Kind Service-forward benchmark smoke (200/200 successful). | Native forward list/inspector and basic GET benchmark; not the complete K9s benchmark configuration surface. |
| Metrics and pulse observation | `client/metrics.go` (`MetricsServer`); `view/{pulse,cluster_info}.go`; `model/pulse.go` | Reads metrics-server node/pod/container data and renders cluster load. | `metrics.list` reads direct client-go `metrics.k8s.io/v1beta1` pod, container, and node usage with canonical quantities; the native inspector shows usage or an explicit unavailable state. No rolling pulse stream/charts. | Partial | Pod/node conversion tests plus Metrics Server available/unavailable integration checks and native unavailable presentation. | Native inspector replaces terminal gauges. |
| Events and diagnostics | `view/event.go`; `client/gvrs.go` (`EvGVR`); `watch` | Shows Kubernetes events and ties diagnostics to resources. | `resource.events` retrieves related events through the typed client and the inspector displays a native timeline. No event watch/retry policy yet. | Partial | Server event tests and Kind fixture diagnostics check. | Native inspector timeline. |
| Scale, restart, rollout | `view/scale_extender.go`; `restart_extender.go`; workload viewers | Scales supported workloads and restarts with K9s semantics. | Native Scale sheet supports selected Deployments, StatefulSets, ReplicaSets, and ReplicationControllers; guarded Restart updates the template timestamp on selected Deployments, StatefulSets, or DaemonSets. A confirmed CronJob Trigger action checks `jobs.create`, then builds an owned Job from the live CronJob template through typed client-go, following K9s manual-job metadata semantics. Workload inspectors summarize live Deployment/StatefulSet/DaemonSet rollout progress, replica counts, revisions, and conditions from the existing resource watch. K9k also mirrors K9s' ReplicaSet rollback flow: only an inactive ReplicaSet with a Deployment controller can be selected; the app access-reviews `deployments.patch`, confirms the operation, and replaces the controller template with that selected revision. Revision-history browsing remains absent. | Partial | Scale validation/error tests, CronJob server/template tests, native rollout-summary smoke, and Kind Deployment scale/restore/restart/ReplicaSet rollback smoke. | Native form/RBAC confirmation replaces terminal prompts. |
| Node operations | `view/node.go` (`drainCmd`, `toggleCordonCmd`, `sshCmd`, `yamlCmd`) | Views node pods/YAML and manages cordon, drain, and shell/SSH. | Native node actions cordon/uncordon through a patch access review. The Drain sheet requires a cordoned node plus explicit confirmation, lists outcome categories, calls only the Kubernetes `policy/v1` eviction API, retains DaemonSet/mirror Pods, honors PDBs, and never force-deletes; node shell/SSH remain absent. | Partial | Drain validation/confirmation IPC test; Kind cordon + non-destructive blocked-drain smoke. | Native progress and per-Pod outcomes, not terminal output. |
| Ownership and reverse relationships | `view/owner_extender.go`; `reference.go`; resource-specific `refCmd`; `internal/xray` | Traverses owners, used-by references, and topology. | `relationships.get` returns a bounded direct-client graph for owner refs, workload dependents, label selectors, Service routing, and declared ConfigMap/Secret/PVC references. The native Relationships sheet makes direction explicit and reports partial-RBAC warnings. | Partial | Deterministic graph unit tests and Kind topology smoke. | Native relationship list/inspector replaces tree navigation. |
| XRay topology | `internal/xray/{generic,pod,dp,sts,ds,rs,svc,ns,sa,container}.go`; `view/xray.go` | Builds live supported-resource topology and node actions. | Native XRay sheet renders a focused workload graph snapshot. It does not yet stream updates, recurse beyond direct edges, draw a canvas, or cover arbitrary CRD relations. | Partial | Deterministic graph unit tests and Kind topology smoke. | Native relationship list/inspector replaces tree navigation. |
| Registered core/apps/batch/RBAC/CRD/Helm routes | `client/gvrs.go`; `view/registrar.go` | Registers specialised resource viewers and K9s virtual views. | Discovery/generic access cover server-advertised resources; specialised registration is absent. | Partial | Discovery fixtures for each API family. | Dynamic browser remains essential for CRDs. |
| RBAC inspection and access checks | `client/client.go` (`CanI`); `view/{rbac,policy,user,group,sa}.go` | Evaluates access and shows roles, bindings, rules, subjects. | `rbac.check` performs direct `SelfSubjectAccessReview` for the active kubeconfig identity. Native inspectors summarize Role/ClusterRole rules, RoleBinding/ClusterRoleBinding role references and subjects, plus ServiceAccount token/image-pull settings. User/group browsing and arbitrary effective-permission analysis remain absent. | Partial | Allowed, denied, malformed-request, and API-error review tests; native RBAC inspector smoke. | Surface results before mutation; retain explicit forbidden errors. |
| Helm releases/history | `view/{helm_chart,helm_history}.go`; `internal/helm` | Uses Helm v3 for chart/release browsing and history. | Native Helm Releases navigation opens the selected cluster's standard Helm storage objects (`owner=helm`) through the direct dynamic client. Release decoding, revision history, and chart actions remain absent. | Partial | Helm storage fixture. | Native release inspector later. |
| Image scan/vulnerabilities | `view/{img_scan,vul_extender}.go`; `internal/vul`; `config/scans.go` | Runs configured scanners and annotates workload/image views. | No scanner integration. | Blocked | Scanner adapter/report normalisation. | Native security inspector. |
| Manifest directories | `view/dir.go`; `dao/dir.go` | Browses manifests and applies, deletes, edits, views, and follows resources. | K9k safely edits an existing selected cluster resource and imports one YAML manifest for the selected discovered GVR (paste or native file picker). Both paths dry-run first, preserve the existing-object UID guard where applicable, require explicit confirmation, and use non-forced SSA. Multi-document/directory browsing, directory apply/delete, diff, and follow remain absent. | Partial | Manifest validation/identity tests, live Kind no-op apply/stale-UID rejection, native ConfigMap editor validation, and native ConfigMap import/create smoke. | Native editor/import today; directory workflow and diff later. |
| Aliases, hotkeys, custom views/jumps, plugins | `config/{alias,hotkey,views,jumps,plugin}.go`; `view/actions.go`; `custom_jump.go` | Loads per-context config, aliases, shortcuts, columns/views/jumps, and scoped plugins. | `config.summary` parses declarations and diagnostics. Settings opens a validated, confirmed, SHA-guarded editor for K9s customization files including `jumps.yaml`; aliases resolve in the catalog, matching custom-view dot-path columns render in native resource tables, and configured custom jumps navigate to discovered target GVRs with source namespace/default/all handling, supported metadata label/annotation templates, and native Kubernetes label/field selectors applied to list and watch calls. Scoped K9s plugins appear only for a selected matching resource, expand K9s placeholders in both command and args, quote separate args safely, show the complete host command, require confirmation, and stream stdout/stderr to a native output sheet. | Partial | Config parser compatibility tests and native settings/plugin-runner smoke. | Native commands/settings; no terminal-key emulation requirement. |
| Read-only/dangerous-action safety | `config/k9s.go` (`IsReadOnly`); `view/actions.go`; `ui/dialog` | Hides/blocks dangerous actions and confirms mutations. | A locally persisted read-only toggle disables native mutations and visible confirmation protects destructive actions; it is not backend-enforced. | Partial | Policy unit and destructive-action UI tests. | Visible risk state, not hidden hotkeys. |
| Exports and clipboard | `view/{screen_dump,table_helper,clipboard}.go`; `table.go` | Saves tables/logs/screens and copies selected data. | The native resource browser copies visible rows as TSV and saves its filtered table as JSON, CSV, or TSV through macOS Save panels; log copy/save and raw JSON copy are also available. Screenshot export remains absent. | Partial | Export-format and pasteboard tests. | Native save panels/pasteboard. |
| Benchmarks | `view/{pf,benchmark}.go`; `internal/perf`; `config/benchmark.go` | Runs/cancels port-forward benchmarks and shows results. | Active K9k loopback forwards expose a native HTTP Benchmark sheet: configurable path, 200-request/concurrency-1 defaults matching K9s, bounded request count, cancellation, and completed/successful/failed/rate/mean-latency/elapsed results. It uses `URLSession` directly rather than requiring `hey`, and intentionally cannot target a non-K9k listener. Persisted K9s benchmark YAML/result files and charts remain absent. | Partial | Native loopback-forward benchmark smoke; cancellation/result model validation. | Native per-forward form/results instead of terminal output/files. |
| Help, command navigation, history | `view/{app,command,help,page_stack,picker}.go`; `view/cmd` | Routes views, maintains history, and shows key hints/help. | `NavigationDestination` names GUI categories; no router/history/help parity. | Partial | Navigation state and keyboard-command tests. | Standard macOS navigation/menus/search supersede prompt. |

## Resource registration trace

`internal/view/registrar.go` is the terminal-view registration authority: namespaces, events, pods, services, nodes, secrets, priority classes, config maps, service accounts, PVCs, workloads, contexts, containers, scans, port forwards, screen dumps, benchmarks, aliases, references, pulse, users/groups, CronJobs/jobs, CRDs, and Helm. `internal/client/gvrs.go` additionally declares PV, endpoints, endpoint slices, HPA, network policies, storage classes, PDB, ingresses, metrics, XRay, policy, and RBAC bindings. K9k must retain discovery for extensions/CRs rather than limiting itself to this fixed list.

## Parity acceptance rule

A row moves to **Complete** only when it has: (1) a versioned helper operation with structured errors and cancellation where streamed; (2) direct Kubernetes libraries, not a required external `kubectl`; (3) an accessible native SwiftUI workflow; (4) read-only/RBAC/confirmation-aware mutation; and (5) automated success/error/cancellation validation.
